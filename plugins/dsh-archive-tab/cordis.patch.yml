# dsh-archive-tab bundle layer: inserts the dsh-archive-tab plugin row.
# Applied when a profile lists this bundle (dsh plugin --profile <name> add).
- insert:
    - id: dsh-archive-tab
      name: dsh-archive-tab
